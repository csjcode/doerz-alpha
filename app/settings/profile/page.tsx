import { Metadata } from "next";
import React from "react";

type DocsAboutPageProps = {};
export const metadata: Metadata = {
  title: "DocsAbout - Doerz.fun",
  description: "DocsAbout for Doerz.fun",
};
const DocsAboutPage = (props: DocsAboutPageProps) => {
  return <div>DocsAbout</div>;
};

export default DocsAboutPage;
